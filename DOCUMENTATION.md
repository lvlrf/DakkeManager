# DakkeManager - مستندات فنی پروژه
## مدیریت متمرکز شعب دکه مارکت

**نسخه:** 1.0.0  
**تاریخ آخرین بروزرسانی:** دسامبر 2024  
**نویسنده:** تیم توسعه  

---

## فهرست مطالب

1. [معرفی پروژه](#1-معرفی-پروژه)
2. [معماری سیستم](#2-معماری-سیستم)
3. [ساختار فایل‌ها](#3-ساختار-فایلها)
4. [پیش‌نیازها](#4-پیشنیازها)
5. [نصب و راه‌اندازی](#5-نصب-و-راهاندازی)
6. [تنظیمات](#6-تنظیمات)
7. [API Reference](#7-api-reference)
8. [ساختار دیتابیس هلو](#8-ساختار-دیتابیس-هلو)
9. [امکانات فعال](#9-امکانات-فعال)
10. [امکانات غیرفعال](#10-امکانات-غیرفعال)
11. [فیچرهای برنامه‌ریزی شده](#11-فیچرهای-برنامهریزی-شده)
12. [مشکلات شناخته شده](#12-مشکلات-شناخته-شده)
13. [نکات فنی مهم](#13-نکات-فنی-مهم)
14. [راهنمای توسعه](#14-راهنمای-توسعه)

---

## 1. معرفی پروژه

### 1.1 هدف
این پروژه برای مدیریت متمرکز چندین شعبه که از نرم‌افزار حسابداری **هلو** استفاده می‌کنند طراحی شده است. از آنجا که هلو API رسمی ندارد، این سیستم مستقیماً با دیتابیس SQL Server ارتباط برقرار می‌کند.

### 1.2 مشکلی که حل می‌کند
- هلو قابلیت مدیریت چند شعبه را ندارد
- هر شعبه دیتابیس مستقل دارد
- نیاز به همگام‌سازی قیمت‌ها، نام‌ها و گروه‌بندی کالاها بین شعب

### 1.3 اجزای سیستم
1. **Holoo API** (`holoo_api.py`) - سرویس واسط روی هر سرور شعبه
2. **Branch Manager** (`branch_manager.py`) - برنامه دسکتاپ مدیریت مرکزی

---

## 2. معماری سیستم

```
┌─────────────────────────────────────────────────────────────────┐
│                     سیستم مدیر مرکزی                            │
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │           Branch Manager (Desktop App)                   │   │
│   │                  Python + PyQt5                          │   │
│   └─────────────────────────────────────────────────────────┘   │
│                              │                                   │
└──────────────────────────────│───────────────────────────────────┘
                               │ HTTP/REST API
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
         ▼                     ▼                     ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   شعبه مدرس     │  │   شعبه شمال     │  │   شعبه جنوب     │
│                 │  │                 │  │                 │
│ ┌─────────────┐ │  │ ┌─────────────┐ │  │ ┌─────────────┐ │
│ │ Holoo API   │ │  │ │ Holoo API   │ │  │ │ Holoo API   │ │
│ │ Port: 7480  │ │  │ │ Port: 7480  │ │  │ │ Port: 7480  │ │
│ └──────┬──────┘ │  │ └──────┬──────┘ │  │ └──────┬──────┘ │
│        │        │  │        │        │  │        │        │
│ ┌──────▼──────┐ │  │ ┌──────▼──────┐ │  │ ┌──────▼──────┐ │
│ │ SQL Server  │ │  │ │ SQL Server  │ │  │ │ SQL Server  │ │
│ │  2008 R2    │ │  │ │  2008 R2    │ │  │ │  2008 R2    │ │
│ │ Holoo DB    │ │  │ │ Holoo DB    │ │  │ │ Holoo DB    │ │
│ └─────────────┘ │  │ └─────────────┘ │  │ └─────────────┘ │
└─────────────────┘  └─────────────────┘  └─────────────────┘
        │                    │                    │
        └────────────────────┴────────────────────┘
                    شبکه داخلی (VPN/Tunnel)
```

### 2.1 جریان داده
1. Branch Manager درخواست HTTP به API شعبه ارسال می‌کند
2. API شعبه با SQL Server متصل می‌شود
3. کوئری اجرا و نتیجه به JSON تبدیل می‌شود
4. پاسخ به Branch Manager برمی‌گردد

---

## 3. ساختار فایل‌ها

```
C:\Program Files\DakkeManager\
│
├── holoo_api.py              # سرویس API (روی سرورهای شعبه)
├── branch_manager.py         # برنامه دسکتاپ (روی سیستم مدیر)
├── branches.toml             # تنظیمات شعب
├── requirements.txt          # وابستگی‌های Python
├── test_connection.py        # تست اتصال به دیتابیس
├── install_service.bat       # نصب سرویس ویندوز
├── uninstall_service.bat     # حذف سرویس ویندوز
├── nssm.exe                  # ابزار مدیریت سرویس
│
├── holoo_api.log             # لاگ API (خودکار ساخته می‌شود)
├── branch_manager.log        # لاگ برنامه مدیریت
├── holoo_api_stdout.log      # خروجی سرویس
└── holoo_api_stderr.log      # خطاهای سرویس
```

---

## 4. پیش‌نیازها

### 4.1 روی سرورهای شعبه

| نرم‌افزار | نسخه | توضیحات |
|-----------|------|---------|
| Windows Server | 2008 R2+ | سیستم عامل |
| SQL Server | 2008 R2+ | دیتابیس هلو |
| Python | 3.8+ | زبان برنامه‌نویسی |
| ODBC Driver | 17 یا 18 | درایور اتصال به SQL |
| NSSM | - | مدیریت سرویس ویندوز |

### 4.2 روی سیستم مدیر

| نرم‌افزار | نسخه | توضیحات |
|-----------|------|---------|
| Windows | 10+ | سیستم عامل |
| Python | 3.8+ | زبان برنامه‌نویسی |
| فونت Dana | - | اختیاری - برای نمایش بهتر |

### 4.3 شبکه

- ارتباط بین شعب از طریق VPN یا Tunnel (MikroTik)
- پورت 7480 باید در فایروال باز باشد
- پورت 1433 (SQL Server) فقط روی localhost

---

## 5. نصب و راه‌اندازی

### 5.1 نصب Python
```cmd
# دانلود از python.org
# حتماً "Add Python to PATH" را تیک بزنید
```

### 5.2 نصب وابستگی‌ها
```cmd
pip install -r requirements.txt
```

### 5.3 نصب ODBC Driver
```
دانلود از:
https://docs.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server

نسخه 17 یا 18 نصب شود
```

### 5.4 تست اتصال
```cmd
python test_connection.py
```

**خروجی موفق:**
```
[موفق] اتصال برقرار شد!
[موفق] تعداد کالاها: 29071
```

### 5.5 نصب سرویس API
```cmd
# با دسترسی Administrator اجرا کنید
install_service.bat
```

### 5.6 اجرای برنامه مدیریت
```cmd
python branch_manager.py
```

---

## 6. تنظیمات

### 6.1 فایل branches.toml

```toml
# تنظیمات عمومی
[settings]
api_key = "holoo_api_secret_key_2024"  # کلید امنیتی API
timeout = 30                            # تایم‌اوت درخواست (ثانیه)
retry_count = 3                         # تعداد تلاش مجدد

# شعبه اول
[[branches]]
name = "مدرس"                           # نام نمایشی شعبه
ip = "192.168.30.30"                    # IP سرور شعبه
database = "Holoo_TestDB"               # نام دیتابیس هلو
user = "321"                            # یوزر SQL Server
password = "123"                        # پسورد SQL Server
port = 7480                             # پورت API

# شعبه دوم
[[branches]]
name = "شمال"
ip = "192.168.31.10"
database = "Holoo_North"
user = "sa"
password = "password123"
port = 7480
```

### 6.2 تغییر API Key

⚠️ **مهم:** API Key باید در دو جا یکسان باشد:

1. فایل `branches.toml` → `[settings].api_key`
2. فایل `holoo_api.py` → متغیر `API_KEY`

### 6.3 پالت رنگ‌ها (در branch_manager.py)

```python
COLORS = {
    "primary": "#15b366",      # سبز دکه مارکت
    "secondary": "#0F8A4F",    # سبز تیره‌تر
    "accent": "#1ADB7A",       # سبز روشن‌تر
    "bg": "#121212",           # پس‌زمینه
    "surface": "#1e1e1e",      # کارت‌ها
    "text": "#e0e0e0",         # متن
    "danger": "#ff4d4d",       # قرمز
    "warning": "#ffaa00",      # نارنجی
}
```

---

## 7. API Reference

### 7.1 Endpoints بدون احراز هویت

#### GET /health
بررسی سلامت سرویس

**Response:**
```json
{
    "success": true,
    "status": "running",
    "version": "1.0.0"
}
```

#### GET /ping
تست سریع اتصال

**Response:**
```json
{
    "success": true,
    "message": "pong"
}
```

### 7.2 Endpoints با احراز هویت

⚠️ همه این Endpointها نیاز به Header زیر دارند:
```
X-API-Key: holoo_api_secret_key_2024
```

و پارامترهای دیتابیس در Body:
```json
{
    "server": "192.168.30.30",
    "database": "Holoo_TestDB",
    "username": "321",
    "password": "123"
}
```

---

#### POST /check/db
بررسی اتصال به دیتابیس

**Response:**
```json
{
    "success": true,
    "status": "DB_CONNECTED"
}
```

**وضعیت‌های خطا:**
- `DB_AUTH_ERROR` - یوزر/پسورد اشتباه
- `DB_NOT_FOUND` - دیتابیس پیدا نشد
- `DB_CONNECTION_ERROR` - خطای اتصال

---

#### POST /articles
دریافت لیست کالاها

**Request Body:**
```json
{
    "server": "...",
    "database": "...",
    "username": "...",
    "password": "...",
    "search": "",        // اختیاری - جستجو
    "limit": 1000        // اختیاری - تعداد
}
```

**Response:**
```json
{
    "success": true,
    "data": [
        {
            "code": "0101073",
            "name": "خودکار آبی صدف",
            "price": 15000,
            "stock1": 100,
            "stock2": 50,
            "group_code": "01",
            "group_name": "نوشت‌افزار",
            "barcode": "",
            "country": "",
            "model": ""
        }
    ],
    "total": 29071,
    "count": 1000
}
```

---

#### POST /article/{code}
دریافت یک کالا

**Response:**
```json
{
    "success": true,
    "data": {
        "code": "0101073",
        "name": "خودکار آبی صدف",
        "price": 15000,
        ...
    }
}
```

---

#### POST /article/{code}/update
ویرایش کالا

**Request Body:**
```json
{
    "server": "...",
    "database": "...",
    "username": "...",
    "password": "...",
    "name": "نام جدید",      // اختیاری
    "price": 20000,           // اختیاری
    "group_code": "02"        // اختیاری
}
```

**Response:**
```json
{
    "success": true,
    "message": "کالا با موفقیت ویرایش شد",
    "affected_rows": 1
}
```

---

#### POST /groups
دریافت لیست گروه‌ها

**Response:**
```json
{
    "success": true,
    "data": [
        {"code": "01", "name": "نوشت‌افزار"},
        {"code": "02", "name": "خوراکی"}
    ]
}
```

---

#### POST /subgroups
دریافت زیرگروه‌ها

**Request Body (اختیاری):**
```json
{
    "group_code": "01"    // فیلتر بر اساس گروه
}
```

---

#### POST /group/add
افزودن گروه جدید

**Request Body:**
```json
{
    "name": "گروه جدید",
    "code": "25"          // اختیاری - خودکار تولید می‌شود
}
```

---

#### POST /group/{code}/update
ویرایش گروه

**Request Body:**
```json
{
    "name": "نام جدید گروه"
}
```

---

#### POST /batch/update
ویرایش دسته‌ای کالاها

**Request Body:**
```json
{
    "items": [
        {"code": "0101073", "price": 20000},
        {"code": "0101074", "name": "نام جدید"},
        {"code": "0101075", "group_code": "02"}
    ]
}
```

**Response:**
```json
{
    "success": true,
    "results": {
        "success": [...],
        "failed": [...]
    },
    "summary": {
        "total": 3,
        "success_count": 2,
        "failed_count": 1
    }
}
```

---

#### POST /stats
آمار کلی دیتابیس

**Response:**
```json
{
    "success": true,
    "data": {
        "total_articles": 29071,
        "articles_with_price": 25000,
        "articles_with_stock": 18000,
        "articles_with_group": 1,
        "total_groups": 21,
        "total_subgroups": 87
    }
}
```

---

## 8. ساختار دیتابیس هلو

### 8.1 جداول اصلی شناسایی شده

#### dbo.ARTICLE (کالاها) ⭐ اصلی‌ترین جدول

| ستون | نوع | توضیح |
|------|-----|-------|
| `A_Code` | varchar(7) | **کلید اصلی** - کد کالا |
| `A_Name` | varchar(255) | نام کالا |
| `Sel_Price` | float | **قیمت فروش اصلی** |
| `Exist` | float | موجودی انبار 1 |
| `Exist2` | float | موجودی انبار 2 |
| `M_Code` | varchar(5) | کد گروه (FK به M_GROUP) |
| `A_Code_C` | varchar | بارکد |
| `A_Country` | varchar | کشور |
| `Model` | varchar | مدل |

#### dbo.M_GROUP (گروه‌های اصلی)

| ستون | نوع | توضیح |
|------|-----|-------|
| `M_groupcode` | varchar | کلید اصلی - کد گروه |
| `M_groupname` | varchar | نام گروه |

#### dbo.S_GROUP (زیرگروه‌ها)

| ستون | نوع | توضیح |
|------|-----|-------|
| `M_groupcode` | varchar | کد گروه اصلی |
| `S_groupcode` | varchar | کد زیرگروه |
| `S_groupname` | varchar | نام زیرگروه |

#### dbo.ArticlePrice (قیمت‌های متعدد)

| ستون | نوع | توضیح |
|------|-----|-------|
| `A_Code` | varchar | کد کالا |
| `SelID` | int | نوع/سطح قیمت |
| `Sel_Price` | float | قیمت |
| `ModifyDate` | datetime | تاریخ تغییر |

#### dbo.FACTART (آیتم‌های فاکتور)

| ستون | نوع | توضیح |
|------|-----|-------|
| `A_Code` | varchar | کد کالا |
| `Price_BS` | float | **قیمت ثبت شده در فاکتور** |

### 8.2 نکات مهم دیتابیس

#### ✅ قطعی و تست شده:
- تغییر `ARTICLE.Sel_Price` قیمت فروش را تغییر می‌دهد
- تغییر `ARTICLE.A_Name` نام کالا را تغییر می‌دهد
- تغییر `ARTICLE.M_Code` گروه کالا را تغییر می‌دهد
- `FACTART.Price_BS` مستقل از قیمت مرجع است (تاریخچه حفظ می‌شود)

#### ⚠️ هشدار:
- در دیتابیس فعلی، **تقریباً همه کالاها گروه ندارند** (M_Code = NULL)
- جداول `Artclass` و `HLP_` خالی هستند
- اختلاف زیادی بین `ARTICLE.Sel_Price` و `ArticlePrice.Sel_Price` وجود دارد

#### 🔍 نیاز به بررسی بیشتر:
- منبع اصلی گروه‌بندی کالاها (شاید از Prefix کد کالا)
- رابطه دقیق `ArticlePrice` با سیستم

### 8.3 آمار دیتابیس نمونه

```
تعداد کل کالاها: 29,071
کالاهای بدون گروه: 29,070
کالاهای با گروه: 1
تعداد گروه‌ها: 21
تعداد زیرگروه‌ها: 87
```

---

## 9. امکانات فعال ✅

### 9.1 سرویس API (holoo_api.py)

| امکان | وضعیت | توضیح |
|-------|-------|-------|
| Health Check | ✅ فعال | بررسی سلامت سرویس |
| اتصال به SQL Server | ✅ فعال | پشتیبانی از ODBC 17/18 |
| دریافت لیست کالاها | ✅ فعال | با صفحه‌بندی و جستجو |
| دریافت یک کالا | ✅ فعال | بر اساس کد |
| ویرایش کالا | ✅ فعال | نام/قیمت/گروه |
| ویرایش دسته‌ای | ✅ فعال | چند کالا همزمان |
| دریافت گروه‌ها | ✅ فعال | - |
| دریافت زیرگروه‌ها | ✅ فعال | - |
| افزودن گروه | ✅ فعال | با کد خودکار |
| ویرایش گروه | ✅ فعال | - |
| آمار کلی | ✅ فعال | - |
| لاگ‌گیری | ✅ فعال | فایل holoo_api.log |
| Retry خودکار | ✅ فعال | 3 بار تلاش |
| Timeout | ✅ فعال | 30 ثانیه |
| API Key | ✅ فعال | امنیت ساده |

### 9.2 برنامه مدیریت (branch_manager.py)

| امکان | وضعیت | توضیح |
|-------|-------|-------|
| بارگذاری تنظیمات TOML | ✅ فعال | - |
| نمایش چراغ وضعیت شعب | ✅ فعال | قرمز/نارنجی/سبز |
| بررسی وضعیت اتصال | ✅ فعال | همه شعب |
| فعال/غیرفعال کردن شعبه | ✅ فعال | چک‌باکس |
| انتخاب شعبه مرجع | ✅ فعال | ستاره |
| دریافت اطلاعات از شعب | ✅ فعال | - |
| نمایش جدول کالاها | ✅ فعال | - |
| مقایسه نام | ✅ فعال | - |
| مقایسه قیمت | ✅ فعال | با فرمت هزارگان |
| مقایسه گروه | ✅ فعال | - |
| فیلتر فقط مغایرت‌ها | ✅ فعال | - |
| جستجو | ✅ فعال | کد و نام |
| صفحه‌بندی | ✅ فعال | همه/50/100/200/500 |
| تنظیم سایز فونت | ✅ فعال | 8-16 |
| تم تاریک | ✅ فعال | رنگ‌های دکه مارکت |
| لاگ‌گیری | ✅ فعال | - |

---

## 10. امکانات غیرفعال ⏸️

امکاناتی که در UI وجود دارند اما هنوز عملکرد ندارند:

| امکان | وضعیت | دلیل |
|-------|-------|------|
| دکمه "افزودن کالا" | ⏸️ غیرفعال | نیاز به تحلیل بیشتر ستون‌های اجباری |
| ستون "غیرفعال" | ⏸️ غیرفعال | نیاز به شناسایی فیلد مربوطه در DB |
| ستون "تغییر" | ⏸️ غیرفعال | نیاز به تکمیل منطق اعمال تغییرات |
| دکمه "اعمال تغییرات" | ⏸️ غیرفعال | pending_changes پر نمی‌شود |
| فیلتر "موجودی" | ⏸️ غیرفعال | در کامبوباکس هست ولی کار نمی‌کند |

---

## 11. فیچرهای برنامه‌ریزی شده 📋

### 11.1 اولویت بالا

| فیچر | توضیح | پیچیدگی |
|------|-------|---------|
| ✏️ ویرایش از جدول | امکان ویرایش مستقیم در ستون "تغییر" | متوسط |
| ✓ اعمال تغییرات | ارسال تغییرات به شعب انتخاب شده | متوسط |
| 📥 Backup قبل از تغییر | ذخیره مقادیر قبلی در فایل | ساده |
| 🔄 همگام‌سازی قیمت | یکسان‌سازی قیمت همه شعب | ساده |

### 11.2 اولویت متوسط

| فیچر | توضیح | پیچیدگی |
|------|-------|---------|
| ➕ افزودن کالا | ساخت کالای جدید در همه شعب | پیچیده |
| 🚫 غیرفعال کردن کالا | تغییر وضعیت کالا | نیاز به تحلیل |
| 📊 گزارش فاکتورها | نمایش فاکتورهای شعب | متوسط |
| 📈 مقایسه موجودی | نمایش اختلاف موجودی | ساده |
| 🏷️ مدیریت زیرگروه‌ها | CRUD زیرگروه‌ها | متوسط |

### 11.3 اولویت پایین

| فیچر | توضیح | پیچیدگی |
|------|-------|---------|
| 📤 Export به Excel | خروجی اکسل از جدول | ساده |
| 📥 Import از Excel | ورود دسته‌ای از اکسل | متوسط |
| 🔔 اعلان تغییرات | نوتیفیکیشن وقتی قیمت تغییر کرد | پیچیده |
| 📅 زمان‌بندی همگام‌سازی | همگام‌سازی خودکار | پیچیده |
| 👥 چند کاربره | سطوح دسترسی | پیچیده |

---

## 12. مشکلات شناخته شده 🐛

### 12.1 مشکلات حل شده ✅

| مشکل | راه‌حل |
|------|-------|
| `OFFSET/FETCH` در SQL 2008 R2 | استفاده از `TOP` و `ROW_NUMBER()` |
| ODBC Driver 18 + Certificate | اضافه کردن `TrustServerCertificate=yes` |
| `db_denydatareader` | حذف از role با `sp_droprolemember` |
| Password Expired | تغییر پسورد یا غیرفعال کردن policy |
| فونت Dana لود نمی‌شود | جستجوی چندگانه در فونت‌ها |

### 12.2 مشکلات باقیمانده ⚠️

| مشکل | وضعیت | راه‌حل پیشنهادی |
|------|-------|-----------------|
| گروه‌بندی کالاها خالی است | 🔍 بررسی | ممکن است از Prefix کد استفاده شود |
| اختلاف ArticlePrice و ARTICLE | 🔍 بررسی | نیاز به تست در UI هلو |
| ستون "تغییر" کار نمی‌کند | ⏳ در انتظار | تکمیل منطق |

---

## 13. نکات فنی مهم ⚠️

### 13.1 SQL Server 2008 R2

این نسخه قدیمی است و محدودیت‌هایی دارد:

```sql
-- ❌ این کار نمی‌کند:
SELECT * FROM Table
OFFSET 10 ROWS FETCH NEXT 20 ROWS ONLY

-- ✅ جایگزین:
SELECT TOP 20 * FROM (
    SELECT *, ROW_NUMBER() OVER (ORDER BY ID) AS RowNum
    FROM Table
) t
WHERE RowNum > 10
```

### 13.2 ODBC Driver 18

نسخه 18 نیاز به SSL دارد. برای غیرفعال کردن:

```python
connection_string = (
    "DRIVER={ODBC Driver 18 for SQL Server};"
    "SERVER=192.168.30.30;"
    "DATABASE=Holoo_TestDB;"
    "UID=321;"
    "PWD=123;"
    "TrustServerCertificate=yes;"  # ⬅️ این خط مهم است
)
```

### 13.3 دسترسی‌های SQL Server

یوزر API باید این role‌ها را داشته باشد:

```sql
USE [Holoo_TestDB];
EXEC sp_addrolemember 'db_datareader', 'YourUser';
EXEC sp_addrolemember 'db_datawriter', 'YourUser';

-- حذف deny (اگر وجود داشت):
EXEC sp_droprolemember 'db_denydatareader', 'YourUser';
EXEC sp_droprolemember 'db_denydatawriter', 'YourUser';
```

### 13.4 Policy پسورد

اگر خطای "password must be changed" گرفتید:

```sql
ALTER LOGIN [YourUser] WITH 
    PASSWORD = 'NewPassword',
    CHECK_POLICY = OFF,
    CHECK_EXPIRATION = OFF;
```

### 13.5 فایروال

پورت‌های مورد نیاز:

| پورت | استفاده | باز در |
|------|---------|--------|
| 7480 | Holoo API | فایروال ویندوز |
| 1433 | SQL Server | فقط localhost |

---

## 14. راهنمای توسعه

### 14.1 اضافه کردن Endpoint جدید

```python
# در holoo_api.py

@app.route('/new-endpoint', methods=['POST'])
@verify_api_key
def new_endpoint():
    params = parse_db_params(request)
    data = request.get_json() or {}
    
    try:
        def do_work():
            conn = get_db_connection(
                params["server"],
                params["database"],
                params["username"],
                params["password"]
            )
            cursor = conn.cursor()
            
            # کوئری شما
            cursor.execute("SELECT ...")
            result = cursor.fetchall()
            
            cursor.close()
            conn.close()
            return result
        
        result = execute_with_retry(do_work)
        
        return jsonify({
            "success": True,
            "data": result
        })
        
    except Exception as e:
        logger.error(f"خطا: {e}")
        return jsonify({
            "success": False,
            "error": str(e)
        }), 500
```

### 14.2 اضافه کردن ستون به جدول

```python
# در branch_manager.py → update_table()

columns = ["کد کالا", "ستون جدید", ...]

# در حلقه پر کردن:
new_item = QTableWidgetItem(str(value))
self.table.setItem(row_idx, col, new_item)
```

### 14.3 تست کردن

```cmd
# تست API مستقیم
curl -X POST http://localhost:7480/articles ^
  -H "Content-Type: application/json" ^
  -H "X-API-Key: holoo_api_secret_key_2024" ^
  -d "{\"server\":\"localhost\",\"database\":\"Holoo_TestDB\",\"username\":\"321\",\"password\":\"123\",\"limit\":10}"
```

### 14.4 دیباگ

```python
# فعال کردن حالت دیباگ Flask
python holoo_api.py --debug

# یا در کد:
app.run(debug=True)
```

---

## پیوست‌ها

### الف. دستورات مفید SQL

```sql
-- لیست کالاها با گروه
SELECT a.A_Code, a.A_Name, a.Sel_Price, mg.M_groupname
FROM ARTICLE a
LEFT JOIN M_GROUP mg ON mg.M_groupcode = a.M_Code;

-- آمار گروه‌بندی
SELECT 
    COUNT(*) AS Total,
    SUM(CASE WHEN M_Code IS NULL THEN 1 ELSE 0 END) AS NoGroup,
    SUM(CASE WHEN M_Code IS NOT NULL THEN 1 ELSE 0 END) AS WithGroup
FROM ARTICLE;

-- بررسی اختلاف قیمت
SELECT 
    a.A_Code,
    a.Sel_Price AS ArticlePrice,
    ap.Sel_Price AS PriceTablePrice
FROM ARTICLE a
LEFT JOIN ArticlePrice ap ON ap.A_Code = a.A_Code AND ap.SelID = 1
WHERE ISNULL(a.Sel_Price, 0) != ISNULL(ap.Sel_Price, 0);
```

### ب. دستورات سرویس

```cmd
# وضعیت سرویس
nssm status HolooAPIService

# توقف
nssm stop HolooAPIService

# شروع
nssm start HolooAPIService

# ریستارت
nssm restart HolooAPIService

# حذف
nssm remove HolooAPIService confirm
```

### ج. ساختار درخواست کامل

```python
import requests

API_URL = "http://192.168.30.30:7480"
API_KEY = "holoo_api_secret_key_2024"

headers = {
    "Content-Type": "application/json",
    "X-API-Key": API_KEY
}

db_params = {
    "server": "192.168.30.30",
    "database": "Holoo_TestDB",
    "username": "321",
    "password": "123"
}

# دریافت کالاها
response = requests.post(
    f"{API_URL}/articles",
    json={**db_params, "limit": 100},
    headers=headers,
    timeout=30
)

data = response.json()
print(data)
```

---

## تاریخچه تغییرات

| نسخه | تاریخ | تغییرات |
|------|-------|---------|
| 1.0.0 | Dec 2024 | نسخه اولیه |

---

**پایان مستندات**

برای سوالات فنی یا گزارش باگ، لاگ‌ها را بررسی کنید:
- `holoo_api.log`
- `branch_manager.log`
